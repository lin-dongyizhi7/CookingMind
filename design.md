# 「食光家」家庭餐饮制作多模态应用设计方案

## 一、应用定位与核心价值

### 1. 定位

基于文心大模型 4.5 的图像、文本、语音、视频多模态理解与生成能力，打造**家庭专属的 “食材 - 食谱 - 烹饪 - 营养” 全流程助手**，适配不同家庭场景（日常简餐、节日聚餐、儿童辅食、老人餐、减脂餐等），让零基础用户也能高效做出适配家庭需求的餐食。

### 2. 核心价值

* **解决食材浪费**：通过食材识别自动匹配食谱，避免 “买了食材不知道做什么” 的问题；
* **降低烹饪门槛**：用动态可视化指导替代传统文字步骤，复杂菜也能 “跟着做”；
* **适配家庭需求**：结合家庭成员年龄、健康状况（如过敏、减脂）定制专属食谱；
* **提升餐饮体验**：联动营养分析、餐后处理建议，让家庭餐饮更健康、便捷。

## 二、核心功能模块设计（结合多模态能力）

### 模块 1：智能食材管理（图像 + 文本 + 数值模态）

#### 功能亮点

* **食材快速录入**：用户拍摄冰箱 / 菜篮食材（如 “青椒、鸡胸肉、土豆”），文心大模型 4.5 图像识别模块自动识别食材种类、数量，支持手动补充文本修正（如 “鸡胸肉是冷冻的”“土豆已存放 3 天”）；
* **保质期提醒**：基于食材类型（如绿叶菜 3-5 天、肉类冷冻 1 个月）生成倒计时提醒，避免变质浪费；
* **食材分类管理**：按 “冷藏 / 冷冻 / 常温” 自动分类，点击食材可查看 “常见搭配”“保存技巧”（如 “西兰花焯水后冷藏更保鲜”）。

#### 技术实现

* 调用文心大模型 4.5 图像理解 API，优化食材识别准确率（支持常见 200 + 家庭食材，含特殊品类如 “内酯豆腐”“紫甘蓝”）；
* 结合食材数据库（含保质期、存储条件数值），通过文本模态生成个性化保存建议。

### 模块 2：多模态智能食谱生成（图像 + 文本 + 语音模态）

#### 功能亮点

* **“现有食材” 食谱推荐**：用户选择已录入的食材（或直接拍摄食材），输入需求（文本 / 语音）如 “30 分钟内做好”“低脂不辣”“适合 3 岁宝宝吃”，模型自动生成 3-5 套适配食谱，每套含：

  * 成品效果图（文心大模型 4.5 图像生成，还原真实餐食质感）；
  * 详细食材清单（标注 “已有的食材”“需补充的食材”，并关联电商平台快速采购链接）；
  * 营养成分表（按家庭人数计算热量、蛋白质、膳食纤维等数值）；
* **“主题食谱” 定制**：支持按场景搜索（如 “中秋家宴 3 菜 1 汤”“减脂期一周午餐”），输入文本需求（如 “不要海鲜”“少放油”），模型动态调整食谱，例如将 “蒜蓉粉丝蒸虾” 替换为 “蒜蓉粉丝蒸鸡胸”；
* **“食谱 DIY” 辅助**：用户输入创意（如 “想把土豆和牛肉做新菜”），模型生成 3 种做法方向（如 “土豆炖牛肉”“香煎土豆牛肉饼”“牛肉土豆汤”），并补充调味建议（如 “加一勺番茄沙司更提鲜”）。

#### 技术实现

* 融合文本（需求描述）、图像（食材照片）、数值（营养数据）模态，调用文心大模型 4.5 多模态生成 API，确保食谱适配性与可行性；
* 语音交互支持方言识别（如四川话 “弄个不辣的菜”），通过 ASR 转文本后传入模型处理。

### 模块 3：动态烹饪指导（视频 + 语音 + 文本模态）

#### 功能亮点

* **“一步一教” 视频演示**：传统文字步骤升级为 “动态分镜视频”，例如 “炒青椒肉丝”：

  * 第一步：模型生成 “鸡胸肉切丝 + 腌制” 视频（标注 “加 1 勺料酒 + 半勺淀粉，抓匀静置 10 分钟”）；
  * 第二步：实时语音提醒 “锅热后再倒油，避免肉丝粘锅”，支持暂停 / 回放，点击视频中食材（如 “淀粉”）可查看 “替代方案”（如 “没有淀粉可用玉米粉”）；
* **“问题实时答疑”**：烹饪中遇到疑问（如 “肉丝炒老了怎么办”“汤太咸了怎么补救”），可拍摄当前状态（如炒老的肉丝）+ 语音提问，模型结合图像与文本快速给出解决方案（如 “加少量温水焖 2 分钟，再淋半勺生抽提鲜”）；
* **“火候 / 时间智能提醒”**：根据食材量调整参数，例如 “2 人份的米饭，水量没过手指 1 指节，电饭煲煮 25 分钟”，并同步手机闹钟提醒。

#### 技术实现

* 调用文心大模型 4.5 视频生成 API，优化烹饪动作的真实感（避免 “抽象步骤”，如明确 “翻炒频率为每秒 1 次”）；
* 实时交互模块采用 “图像捕捉 + 语音识别” 双输入，确保问题理解准确率（支持识别食材状态，如 “糊底”“夹生”）。

### 模块 4：家庭营养与餐后管理（文本 + 数值 + 图像模态）

#### 功能亮点

* **家庭营养档案**：录入家庭成员信息（如 “爸爸：35 岁，减脂中；宝宝：2 岁，过敏鸡蛋”），模型自动标注食谱中 “禁忌食材”（如含鸡蛋的食谱标注 “宝宝不可食用”），并生成每日营养报表（如 “今日家庭摄入蛋白质 75g，达标；蔬菜摄入不足，建议晚餐加一份清炒时蔬”）；
* **剩菜利用建议**：拍摄剩菜（如 “剩余半碗米饭、少量红烧肉”），模型生成 “剩菜改造食谱”（如 “红烧肉炒饭”“米饭团子 + 红烧肉酱汁”），并提醒 “剩菜冷藏不超过 24 小时”；
* **餐后清洁技巧**：根据餐具类型（如 “沾了油污的炒锅”“装过鸡蛋羹的瓷碗”），生成图文清洁指南（如 “用热水 + 小苏打擦炒锅，避免用钢丝球刮伤涂层”）。

#### 技术实现

* 营养数据模块对接权威营养数据库，结合文心大模型 4.5 文本生成能力，输出易懂的营养建议；
* 剩菜识别优化：支持识别 “混合食材”（如 “米饭 + 红烧肉”），避免单一食材识别局限。

## 三、技术架构设计

### 1. 模型层

* 核心依赖：文心大模型 4.5 多模态系列（图像识别 API、文本生成 API、视频生成 API、语音交互 API）；
* 辅助模型：食材分类模型（优化小众食材识别）、营养计算模型（按中国居民膳食指南校准数据）。

### 2. 应用层

* 前端：支持 iOS/Android/ 小程序，交互设计以 “图文 + 语音” 为主，避免复杂操作（如老人 / 儿童也能通过语音调用 “宝宝辅食食谱”）；
* 后端：采用云原生架构，分模块部署（食材管理模块、食谱生成模块、烹饪指导模块），确保高并发下的响应速度（如晚餐高峰期调用模型延迟≤1 秒）。

### 3. 数据层

* 存储：用户食材数据、家庭营养档案采用加密存储，符合隐私保护规范；
* 缓存：高频食谱（如 “番茄炒蛋”“蛋炒饭”）本地缓存，减少模型调用次数，降低流量消耗。

## 四、创新点与差异化优势

1. **多模态深度融合**：区别于传统 “文字食谱 APP”，首次将 “食材图像识别 + 动态视频指导 + 语音实时答疑” 结合，覆盖家庭餐饮全流程；
2. **家庭专属适配**：支持多人需求同步满足（如同一餐兼顾减脂爸爸、过敏宝宝），而非单一用户视角；
3. **低门槛交互**：无需打字，通过 “拍一拍 + 说说话” 即可完成操作，适配老人、儿童等非熟练用户；
4. **实用性导向**：聚焦 “解决实际问题”（如剩菜利用、食材浪费），而非单纯推荐食谱，提升用户粘性。

## 五、项目实施计划

| 阶段       | 时间区间    | 核心任务                                                                                                                        | 交付物                                          |
| ---------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------- |
| 需求验证期 | 第 1-2 周   | 1. 调研 50 户家庭餐饮痛点；2. 确定文心大模型 API 调用方案；3. 完成核心功能原型设计                                              | 1. 痛点调研报告；2. API 调用文档；3. 产品原型图 |
| 核心开发期 | 第 3-8 周   | 1. 实现食材识别、基础食谱生成功能；2. 开发烹饪指导视频生成模块；3. 完成前端交互开发                                             | 1. 最小可行产品（MVP）；2. 核心模块测试报告     |
| 优化迭代期 | 第 9-12 周  | 1. 优化食材识别准确率（新增 50 + 食材类型）；2. 增加家庭营养档案、剩菜利用功能；3. 收集用户反馈并迭代                           | 1. 1.0 正式版 APP；2. 用户反馈分析报告          |
| 推广运营期 | 第 13-16 周 | 1. 上线应用商店；2. 发起 “家庭厨艺挑战赛”（用户分享用 APP 做的餐食，赢取食材礼包）；3. 迭代 2.0 版本（新增 “节日专题食谱”） | 1. 应用下载量 1 万 +；2. 2.0 版本更新日志       |

> （注：文档部分内容可能由 AI 生成）
